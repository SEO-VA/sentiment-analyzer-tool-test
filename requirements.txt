# requirements.txt

# Core dependencies
streamlit>=1.28.0
openai>=1.0.0

# Web scraping and parsing
requests>=2.28.0
beautifulsoup4>=4.11.0
lxml>=4.9.0

# Browser automation for JS-heavy sites
selenium>=4.15.0
webdriver-manager>=3.8.0

# PDF generation with fallback options
# Primary: WeasyPrint (best quality, needs system deps)
weasyprint>=60.0

# Fallback: ReportLab (pure Python, always works)
reportlab>=4.0.0

# Additional fallback options (uncomment if needed)
# pdfkit>=1.0.0  # Requires wkhtmltopdf system binary
# xhtml2pdf>=0.2.0  # Pure Python but limited CSS support